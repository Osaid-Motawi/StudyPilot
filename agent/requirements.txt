# StudyPilot Fetch AI agent dependencies (Python 3.11)
uagents>=0.15,<1.0
openai>=1.30,<2.0
python-dotenv>=1.0,<2.0

# Testing (ASI:One client is mocked; no network / real key required)
pytest>=8.0,<9.0
