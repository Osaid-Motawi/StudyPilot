rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All data access goes through the backend (Firebase Admin SDK), which
    // bypasses these rules. Direct client access is denied as defense-in-depth.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
